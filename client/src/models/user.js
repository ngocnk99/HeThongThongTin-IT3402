export default class User {
    constructor(username, email, password, book) {
        this.username = username;
        this.email = email;
        this.password = password;
        this.book = book;
    }
}